# TTS Configuration
TTS_LANGUAGE=en                    # en or ru
TTS_MODE=static                    # static or dynamic

# TTS Providers (in priority order)
ELEVENLABS_API_KEY=                # Best quality, sci-fi voices
OPENAI_API_KEY=                    # Good quality, required for dynamic mode
# pyttsx3 is used as offline fallback if no API keys are set
